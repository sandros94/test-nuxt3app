{
    "private": true,
    "engines": {
        "node": ">=18.0.0 <19.0.0"
    },
    "scripts": {
        "clear": "nuxt cleanup",
        "build": "nuxt build",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare"
    },
    "devDependencies": {
        "@nuxthq/ui": "^2.1.0",
        "nuxt": "^3.5.1",
        "nuxt-directus": "^5.3.0"
    }
}
